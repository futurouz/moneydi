import React from 'react';
import Navbar from './common/Navbar';
import GeneralFrom from './GeneralForm';

const Form = () => {
    return (
        <div>
            <Navbar/>
            <GeneralFrom/>
        </div>
    )
};

export default Form